return {
    playerInteraction = function(npc)
        npc:say("there will be worse things\ncoming for all of us", {"what", "no"})
        npc:say("chunks are in*finite*\nde*finite*ly gonna take a look\npenis!")
    end
}